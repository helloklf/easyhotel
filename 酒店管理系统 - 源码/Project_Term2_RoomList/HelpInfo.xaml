<UserControl x:Class="Project_Term2_RoomList.HelpInfo" x:Name="this" Foreground="{Binding ForeColor}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
        <StackPanel>
            <TextBlock FontSize="20" Padding="25" Foreground="{Binding Foreground,ElementName=C}">关于员工管理：</TextBlock>
            <TextBlock TextWrapping="Wrap" Padding="20,0,20,0" FontSize="17" Foreground="{Binding Foreground,ElementName=C}" Margin="35">
                            　　房间管理功能仅向房间管理员开放。在这里，提供房间的新增、房间信息修改、房间状态更改、房间记录禁用和启用。
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>
